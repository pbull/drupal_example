version: 1.0.0
events:
  build:
    steps:
      - hello:
          type: script
          script:
            - mkdir docroot
            - echo "Hello. My name is Inigo Montoya. You killed my build process. Prepare to die." > docroot/hello.txt
      - deploy:
          script:
            - pipelines-deploy

